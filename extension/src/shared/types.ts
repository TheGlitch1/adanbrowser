// Shared domain types used across all layers.
// Import these in domain/, application/, infrastructure/, and UI entry points.

export type PrayerName = 'fajr' | 'dhuhr' | 'asr' | 'maghrib' | 'isha';
