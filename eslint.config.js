// ESLint flat config (ESLint 9+)
// To enable TypeScript-aware linting, install typescript-eslint:
//   npm i -D typescript-eslint
// Then extend this config with: ...tseslint.configs.recommended

export default [
  {
    ignores: ['dist/**', 'node_modules/**'],
  },
];
